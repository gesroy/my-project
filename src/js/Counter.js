/*export default class Counter {
  constructor() {}

  setupEvents() {
    this.el.addEventListener('click', event => {
      this.countBalls()
      event.stopPropagation()
      this.destroy()
    })
  }

  countBalls(event) {
    return +1
  }

  destroy() {
    this.el.remove()
    this.el = null
  }
}*/
